// @ts-check

/** @type {import('tailwindcss').Config['theme']} */
const theme = {
    extend: {
        colors: {
            primary: {
                light: '#2F5651',
                DEFAULT: '#2F5651',
                dark: '#F1EDE1',
            },
            secondary: {
                light: '#F1EDE1',
                DEFAULT: '#F1EDE1',
            },
            brown: {
                light: '#CBA780',
                DEFAULT: '#CBA780',
            },
        },
    },
}

module.exports = {
    theme,
}
