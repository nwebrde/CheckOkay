version: "3"

services:
  web:
    platform: linux/arm64
    image: ghcr.io/nikwebr/checkokay:latest
    container_name: next
    build:
      context: .
      dockerfile: ./apps/next/Dockerfile
    restart: always
    ports:
      - 3000:3000